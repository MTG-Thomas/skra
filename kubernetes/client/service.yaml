apiVersion: v1
kind: Service
metadata:
  name: client
  namespace: skra
  labels:
    app.kubernetes.io/name: skra-client
    app.kubernetes.io/component: client
    app.kubernetes.io/part-of: skra
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: skra-client
