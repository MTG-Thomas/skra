# Deny all ingress to skra by default
# Pods must be explicitly allowed
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-ingress
  namespace: skra
spec:
  podSelector: {}
  policyTypes:
    - Ingress
