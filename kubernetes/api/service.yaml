apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: skra
  labels:
    app.kubernetes.io/name: skra-api
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: skra
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: skra-api
