apiVersion: apps/v1
kind: Deployment
metadata:
  name: skra-worker
  namespace: skra
  labels:
    app.kubernetes.io/name: skra-worker
    app.kubernetes.io/component: worker
    app.kubernetes.io/part-of: skra
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: skra-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: skra-worker
        app.kubernetes.io/component: worker
        app.kubernetes.io/part-of: skra
    spec:
      containers:
        - name: worker
          image: ghcr.io/mtg-thomas/skra-api:latest
          command: ["arq", "src.worker.WorkerSettings"]
          envFrom:
            - configMapRef:
                name: skra-config
            - secretRef:
                name: skra-secrets
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          # Worker doesn't have HTTP endpoint, use exec probe
          livenessProbe:
            exec:
              command:
                - python
                - -c
                - "import redis; r = redis.from_url('${SKRA_REDIS_URL}'); r.ping()"
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
      # Run as non-root user (matches Dockerfile)
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
